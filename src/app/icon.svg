<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="32" height="32">
  <!-- Fundo escuro -->
  <rect width="32" height="32" rx="6" fill="#0A0A0A"/>
  <!-- Listra 1 — azul (topo) -->
  <rect x="6" y="8" width="20" height="5" rx="2" fill="#1E7FC0"/>
  <!-- Listra 2 — vermelha (meio, mais curta) -->
  <rect x="6" y="15" width="14" height="4" rx="2" fill="#E03C31"/>
  <!-- Listra 3 — azul (base) -->
  <rect x="6" y="21" width="20" height="5" rx="2" fill="#1E7FC0"/>
</svg>
